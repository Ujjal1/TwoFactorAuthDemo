<%= form_with model: @user, url: account_settings_path, method: :patch do |f| %>
    <%= f.label :email %>
    <%= f.text_field :email, required: true %>
    <%= f.label :password %>
    <%= f.text_field :password, required: true %>
    <%= f.submit 'Update Account Settings' %>
  <% end %>